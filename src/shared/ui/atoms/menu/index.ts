import { Menu as Wrapper } from './menu'
import { Button } from './button'
import { Item } from './item'
import { Items } from './items'

export const Menu = {
  Wrapper,
  Button,
  Items,
  Item,
}
